package com.gcu;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ECommerceLoginModuleApplication {

    public static void main(String[] args) {
        // This is the main method that starts the Spring Boot application.
        // It uses the SpringApplication.run method to bootstrap the application.
        SpringApplication.run(ECommerceLoginModuleApplication.class, args);
    }

}
